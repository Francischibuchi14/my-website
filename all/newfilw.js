function boolGood(y, x, v) {
    let total_worth = y + x + v
    if(total_worth > 50){
        return total_worth;
    }
}
let total_worth = boolGood(70 ,60 ,40);
 if( total_worth === undefined){
    console.log(`sum doesb not have any output`);
 }else{
    
 }